# Environment specific values for csm-cloud-job.
# Declare variables to be passed into your templates.

stage: sandbox4
profiles: staging

# configuration for KV Flex volumes
env: sandbox4
subscription: sandbox
subscriptionid: "REPLACE_ME"
tenantid: "REPLACE_ME"
