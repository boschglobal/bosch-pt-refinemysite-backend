logging:
  level:
    org:
      springframework:
        test:
          context:
            # change to debug to get infos about testcontext caching and reusing
            # cache: debug
            transaction:
              # to log the @Transactional lifecycle (start / rollback) change the following line
              TransactionContext: warn