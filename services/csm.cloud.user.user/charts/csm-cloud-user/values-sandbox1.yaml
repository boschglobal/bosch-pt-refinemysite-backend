# Environment specific values for csm-cloud-user.
# Declare variables to be passed into your templates.

stage: sandbox1
profiles: staging

# configuration for KV Flex volumes
env: sandbox1
subscription: sandbox
subscriptionid: "REPLACE_ME"
tenantid: "REPLACE_ME"
