# Environment specific values for csm-cloud-project-restore-db.
# Declare variables to be passed into your templates.

stage: review
profiles: production, restore-db, idp-bosch-dev, init-topics

# configuration for KV Flex volumes
env: review
subscription: qa
subscriptionid: "REPLACE_ME"
tenantid: "REPLACE_ME"
