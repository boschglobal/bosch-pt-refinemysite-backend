kubernetes:
  informer:
    enabled: false
  manifests:
    enabled: false
spring:
  application:
    name: csm-cloud-project
  main:
    banner-mode: off
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
  cloud:
    kubernetes:
      enabled: false
